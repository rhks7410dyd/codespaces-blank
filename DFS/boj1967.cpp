#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int n;
vector<vector<pair<int,int>>> v;

int main(){
	cin.tie(NULL);
	cout.tie(NULL);
	ios::sync_with_stdio(false);
	
	cin >> n;
	
	int a,b,r;
	for(int i = 1 ; i < n ; i++){
		cin >> a >> b >> r;
	}
	
	
	
	return 0;
}