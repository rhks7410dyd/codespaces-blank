#include <iostream>
#include <cmath>

using namespace std;

int n;
double info[100][3];

int main(){
	cin.tie(NULL);
	cout.tie(NULL);
	ios_base::sycn_with_stdio(false);
	
	int c;
	cin >> c;
	while(c--){
		cin >> n;
		for(int i = 0 ; i < n ; i++){
			cin >> info[0] >> info[1] >> info[2];
		}
		
		
	}
	
	return 0;
}