#include <iostream>

using namespace std;

int main(){
	cin.tie(NULL);
	cout.tie(NULL);
	ios_base::sycn_with_stdio(false);
	
	unsigned long long a,b;
	cin >> a >> b;
	
	//이거 이렇게하면 무조건 시간 초과임.
	for(unsigned long long i = 1 ; i < b/2 + 1 ; i++){
		
	}
	
	return 0;
}