#include <iostream>

using namespace std;

int n,h,w,r[100],c[100];
bool paper[101][101];

int main(){
	cin.tie(NULL);
	cout.tie(NULL);
	ios_base::sync_with_stdio(false);
	
	cin >> h >> w >> n;
	for(int i = 0 ; i < n ; i++){
		cin >> r[i] >> c[i]
	}
	
	for(int i = 0 ; i < n ; i++){
		for(int j = i+1 ; j < n ; j++){
			
		}
	}
	
	return 0;
}